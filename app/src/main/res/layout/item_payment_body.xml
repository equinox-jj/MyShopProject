<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/clPaymentBody"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/white_background"
    android:padding="8dp">

    <ImageView
        android:id="@+id/ivImagePayment"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/img_bca" />

    <TextView
        android:id="@+id/tvPaymentName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        android:maxLines="1"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="@+id/ivImagePayment"
        app:layout_constraintEnd_toStartOf="@+id/ivForwardBtn"
        app:layout_constraintStart_toEndOf="@+id/ivImagePayment"
        app:layout_constraintTop_toTopOf="@+id/ivImagePayment"
        tools:text="Lorem Ipsum" />

    <ImageView
        android:id="@+id/ivForwardBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@null"
        android:src="@drawable/ic_forward_filled"
        app:layout_constraintBottom_toBottomOf="@+id/ivImagePayment"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/ivImagePayment" />

</androidx.constraintlayout.widget.ConstraintLayout>